# fha_central_data
To handle initial data fetching, cleaning, and standard processing


# install virtual environment
conda env create -f environment.yml
conda env update -n fha_data -f environment.yml --prune